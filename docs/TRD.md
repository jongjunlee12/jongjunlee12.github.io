# ⚙️ [Isochrone Map Viewer] 기술 사양서 (TRD)

> **버전**: 1.1 (API 키 불필요 버전)
> **작성일**: 2025-11-30
> **문서 유형**: 기술 사양서 (Technical Requirements Document)

---

## 1. 기술 스택 요약

| 분류 | 기술 | 버전 | 선정 이유 |
|------|------|------|-----------|
| **언어** | Python | 3.11+ | AI 코딩 친화적, 풍부한 지리 라이브러리 |
| **프레임워크** | Streamlit | 1.51.0 | 개인용 도구에 최적, 빠른 프로토타이핑 |
| **도로 네트워크** | OSMnx | 2.0.7 | OpenStreetMap 데이터 다운로드 및 그래프 분석 |
| **그래프 분석** | NetworkX | 3.4+ | 최단 경로 및 도달 가능 영역 계산 |
| **지리 데이터** | GeoPandas | 1.0+ | 폴리곤 생성 및 GeoJSON 변환 |
| **지도 시각화** | Folium | 0.19.0 | Leaflet 기반, Streamlit 통합 |
| **Streamlit-Folium** | streamlit-folium | 0.25.3 | 양방향 지도 컴포넌트 |
| **형상 처리** | Shapely | 2.0+ | Convex Hull 폴리곤 생성 |
| **지도 타일** | CartoDB Positron | - | 밝은 회색 스타일 (내장) |

---

## 2. 선정 이유 상세

### 🎯 왜 OSMnx + NetworkX인가?

| 기준 | 평가 |
|------|------|
| **API 키 불필요** | ✅ OpenStreetMap Overpass API는 무료, 키 불필요 |
| **무제한 사용** | ✅ 요청 횟수 제한 없음 (합리적 사용 시) |
| **로컬 처리** | ✅ 다운로드 후 모든 계산은 로컬에서 수행 |
| **정확도** | ✅ 실제 도로 네트워크 기반 계산 |
| **유연성** | ✅ 도보/차량 속도 직접 설정 가능 |

### 🗺️ 계산 방식 비교

| 방식 | API 키 | 장점 | 단점 |
|------|--------|------|------|
| OpenRouteService | 필요 | 빠름, 정확 | 일일 제한 있음 |
| **OSMnx (선택)** | **불필요** | **무제한, 무료** | 첫 로딩 느림 |
| Valhalla 셀프호스팅 | 불필요 | 빠름 | 서버 구축 필요 |

---

## 3. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                    시스템 아키텍처 (로컬 처리)                    │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐      ┌──────────────┐      ┌──────────────────┐
│              │      │              │      │   OpenStreetMap  │
│   사용자      │◄────►│  Streamlit   │◄────►│   Overpass API   │
│  (브라우저)   │      │    App       │      │  (도로망 다운로드) │
│              │      │              │      │                  │
└──────────────┘      └──────┬───────┘      └──────────────────┘
                             │
                             ▼
                      ┌──────────────┐
                      │    OSMnx     │
                      │ (그래프 생성) │
                      └──────┬───────┘
                             │
                             ▼
                      ┌──────────────┐
                      │  NetworkX    │
                      │ (경로 계산)   │
                      └──────┬───────┘
                             │
                             ▼
                      ┌──────────────┐
                      │   Shapely    │
                      │ (폴리곤 생성) │
                      └──────┬───────┘
                             │
                             ▼
                      ┌──────────────┐
                      │   Folium     │
                      │  (지도 렌더링) │
                      └──────┬───────┘
                             │
                             ▼
                      ┌──────────────┐
                      │  CartoDB     │
                      │  Positron    │
                      │  (타일 서버)  │
                      └──────────────┘
```

### 데이터 흐름

```
[사용자 입력]
     │
     ▼
┌─────────────────────────────────────────┐
│ 1. 좌표 (lat, lon)                       │
│ 2. 이동수단 (walk / drive)               │
│ 3. 시간 (분 단위)                         │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│   OSMnx: 좌표 주변 도로 네트워크 다운로드   │
│   ox.graph_from_point((lat, lon), ...)  │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│   NetworkX: 이동 시간 기반 도달 노드 탐색  │
│   nx.ego_graph(G, center_node, ...)     │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│   Shapely: 도달 노드들로 폴리곤 생성      │
│   ConvexHull 또는 ConcaveHull           │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│   Folium: GeoJson으로 지도에 표시        │
│   folium.GeoJson(polygon).add_to(map)   │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│      st_folium()로 Streamlit 렌더링      │
└─────────────────────────────────────────┘
```

---

## 4. 프로젝트 디렉토리 구조

```
isochrone-map-viewer/
│
├── app.py                  # 메인 Streamlit 애플리케이션 (단일 파일)
├── requirements.txt        # Python 의존성 목록
├── .gitignore             # Git 제외 파일 목록
│
├── .streamlit/
│   └── config.toml        # Streamlit 설정 (테마 등)
│
└── README.md              # 프로젝트 설명 및 실행 방법
```

> ⚠️ **중요**: 개인용 도구이므로 단일 파일(`app.py`) 구조를 권장합니다.
> `.env` 파일은 불필요합니다 (API 키가 없으므로).

---

## 5. 필수 라이브러리 (requirements.txt)

```txt
# Core Framework
streamlit==1.51.0

# OpenStreetMap & Graph Analysis
osmnx==2.0.7
networkx>=3.4

# Geospatial Data Processing
geopandas>=1.0.0
shapely>=2.0.0

# Map Visualization
folium==0.19.0
streamlit-folium==0.25.3

# Numerical Computing (OSMnx 의존성)
numpy>=1.26.0
pandas>=2.2.0
```

---

## 6. 환경 설정

### .streamlit/config.toml

```toml
[theme]
primaryColor = "#4A90D9"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F0F2F6"
textColor = "#262730"
font = "sans serif"

[server]
headless = true
port = 8501
maxUploadSize = 200
```

### .gitignore

```gitignore
# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OSMnx cache
cache/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
```

---

## 7. 핵심 알고리즘: 아이소크론 계산

### Step 1: 도로 네트워크 다운로드

```python
import osmnx as ox

# 네트워크 타입: 'walk' (도보) 또는 'drive' (차량)
G = ox.graph_from_point(
    center_point=(위도, 경도),
    dist=3000,              # 반경 3km 내 도로망
    network_type='walk'     # 'walk' 또는 'drive'
)
```

### Step 2: 이동 시간 계산 (엣지에 travel_time 속성 추가)

```python
# 도보 속도: 약 4.5 km/h = 1.25 m/s
# 차량 속도: 도로 제한속도 또는 기본값 사용

# 도보의 경우: 직접 계산
for u, v, data in G.edges(data=True):
    length = data['length']  # 미터 단위
    speed = 1.25             # m/s (도보)
    data['travel_time'] = length / speed  # 초 단위

# 차량의 경우: OSMnx 내장 함수 사용
G = ox.routing.add_edge_speeds(G)
G = ox.routing.add_edge_travel_times(G)
```

### Step 3: 도달 가능 노드 탐색

```python
import networkx as nx

# 시작점에서 가장 가까운 노드 찾기
center_node = ox.nearest_nodes(G, 경도, 위도)  # 주의: (X, Y) = (lon, lat)

# 지정 시간 내 도달 가능한 서브그래프
trip_time = 15 * 60  # 15분 = 900초
subgraph = nx.ego_graph(G, center_node, radius=trip_time, distance='travel_time')
```

### Step 4: 폴리곤 생성

```python
from shapely.geometry import Point, MultiPoint
from shapely.ops import unary_union

# 도달 가능 노드들의 좌표 추출
node_coords = [(G.nodes[node]['x'], G.nodes[node]['y']) for node in subgraph.nodes()]

# Convex Hull로 폴리곤 생성
points = MultiPoint([Point(coord) for coord in node_coords])
isochrone_polygon = points.convex_hull
```

### Step 5: 지도에 표시

```python
import folium
import geopandas as gpd

m = folium.Map(
    location=[위도, 경도],
    tiles="CartoDB Positron",
    zoom_start=14
)

# GeoJSON으로 변환하여 추가
folium.GeoJson(
    isochrone_polygon.__geo_interface__,
    style_function=lambda x: {
        'fillColor': '#3388ff',
        'color': '#3388ff',
        'weight': 2,
        'fillOpacity': 0.3
    }
).add_to(m)
```

---

## 8. 이동 수단별 설정

| 이동 수단 | network_type | 속도 | 비고 |
|-----------|--------------|------|------|
| **도보** | `'walk'` | 4.5 km/h (1.25 m/s) | 보도, 횡단보도 포함 |
| **차량** | `'drive'` | 도로별 제한속도 | OSMnx 자동 계산 |

### 속도 설정 코드

```python
def add_travel_time(G, mode='walk'):
    """그래프 엣지에 이동 시간 추가"""
    if mode == 'walk':
        # 도보: 4.5 km/h = 1.25 m/s
        walk_speed = 1.25
        for u, v, data in G.edges(data=True):
            data['travel_time'] = data['length'] / walk_speed
    else:
        # 차량: OSMnx 내장 함수 사용 (도로 제한속도 기반)
        G = ox.routing.add_edge_speeds(G)
        G = ox.routing.add_edge_travel_times(G)
    return G
```

---

## 9. 핵심 코드 패턴

### 지도 생성 (Carto Positron 스타일)

```python
import folium

m = folium.Map(
    location=[위도, 경도],
    tiles="CartoDB Positron",  # 밝은 회색 스타일
    zoom_start=14
)
```

### 중심점 마커 추가

```python
folium.Marker(
    location=[위도, 경도],
    popup="출발점",
    icon=folium.Icon(color='red', icon='info-sign')
).add_to(m)
```

### Streamlit에서 지도 표시

```python
from streamlit_folium import st_folium

st_folium(m, width=700, height=500)
```

### 캐싱으로 성능 최적화

```python
@st.cache_data(ttl=3600)  # 1시간 캐시
def get_graph(lat, lon, dist, network_type):
    """도로 네트워크 다운로드 (캐싱)"""
    return ox.graph_from_point(
        center_point=(lat, lon),
        dist=dist,
        network_type=network_type
    )
```

---

## 10. AI 코딩 주의사항

### ✅ 반드시 지켜야 할 것

| # | 주의사항 |
|---|----------|
| 1 | **좌표 순서**: OSMnx `graph_from_point`는 `(lat, lon)`, `nearest_nodes`는 `(lon, lat)` 순서! |
| 2 | **시간 단위**: 내부 계산은 초(seconds) 단위, UI는 분(minutes) 단위 |
| 3 | **캐싱 필수**: `@st.cache_data`로 네트워크 다운로드 캐싱 (성능 핵심) |
| 4 | **단일 파일 유지**: `app.py` 하나로 모든 기능 구현 |
| 5 | **에러 처리**: 네트워크 다운로드 실패 시 사용자 친화적 메시지 표시 |
| 6 | **로딩 표시**: `st.spinner()`로 계산 중임을 사용자에게 알림 |

### ❌ 하지 말아야 할 것

| # | 금지사항 |
|---|----------|
| 1 | 불필요한 모듈/파일 분리 |
| 2 | 외부 API 키 사용 시도 |
| 3 | 사양서에 없는 라이브러리 추가 |
| 4 | `ox.graph_from_point`에서 `(lon, lat)` 순서 사용 (반대임!) |
| 5 | 캐싱 없이 매번 네트워크 다운로드 |

---

## 11. 성능 최적화 팁

| 최적화 | 설명 |
|--------|------|
| **네트워크 캐싱** | `@st.cache_data`로 다운로드 결과 캐싱 |
| **적절한 반경** | 도보 15분 = 약 1.5km, 반경 2km면 충분 |
| **세션 상태 활용** | `st.session_state`로 그래프 재사용 |

### 권장 다운로드 반경

| 시간 | 도보 (4.5km/h) | 차량 (30km/h 가정) | 권장 반경 |
|------|----------------|-------------------|-----------|
| 5분 | 375m | 2.5km | 1km / 4km |
| 15분 | 1.1km | 7.5km | 2km / 10km |
| 30분 | 2.3km | 15km | 3km / 18km |
| 60분 | 4.5km | 30km | 6km / 35km |

---

## 12. 배포 가이드

### 로컬 실행 (권장)

```bash
# 1. 가상환경 생성
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. 의존성 설치
pip install -r requirements.txt

# 3. 실행
streamlit run app.py
```

### Streamlit Community Cloud 배포 (주의)

> ⚠️ **주의**: OSMnx는 메모리를 많이 사용합니다.
> Streamlit Cloud 무료 티어(1GB RAM)에서는 큰 영역 계산 시 메모리 초과 가능.
> 로컬 실행을 권장합니다.

---

## 13. 참고 자료

| 자료 | 링크 |
|------|------|
| OSMnx 공식 문서 | https://osmnx.readthedocs.io |
| OSMnx Isochrone 예제 | https://github.com/gboeing/osmnx-examples |
| Geoff Boeing 블로그 | https://geoffboeing.com/2017/08/isochrone-maps-osmnx-python/ |
| NetworkX 문서 | https://networkx.org/documentation/stable/ |
| Streamlit 공식 문서 | https://docs.streamlit.io |
| Folium 공식 문서 | https://python-visualization.github.io/folium |
| streamlit-folium | https://folium.streamlit.app |
| CartoDB 베이스맵 | https://github.com/CartoDB/basemap-styles |
